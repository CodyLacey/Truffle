<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Truffle</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&family=Source+Sans+Pro&display=swap" rel="stylesheet">
    <link rel="shortcut icon" href="images/logo.ico" type="image/x-icon">
    <link rel="icon" href="images/logo.ico" type="image/x-icon">
    <link rel="stylesheet" href="css/normalize.css" />
    <link rel="stylesheet" href="css/style.css" />
    <script src="https://kit.fontawesome.com/560ecaf078.js" crossorigin="anonymous"></script>
</head>

<body class="back">
    <section class="page">
        <nav>
            <a href="#" class="top">
                <i class="fas fa-book"></i>
                <span class="tool tip1">Recipes</span>
            </a>
            <a href="#" class="search">
                <i class="fas fa-search"></i>
                <span class="tool tip2">Search</span>
            </a>
            <a href="#" class="cart">
                <i class="fas fa-shopping-cart"></i>
                <span class="tool tip3">Grocery</span>
            </a>
            <a href="#" class="bottom">
                <i class="fas fa-sign-out-alt"></i>
                <span class="tool tip4">Logout</span>
            </a>
        </nav>
        <section>
            <h1 class="truffle smallTruffle">Truffle</h1>
            <form class="form smallForm" action="/grabRecipe" method="POST">
                <input class="importRecipe smallInput" type="text" name="url" placeholder="Paste the URL of a recipe" id="">
                <button class="importRecipe2 smallButton" type="submit" value="Import" name="" id=""><i class="fas fa-search"></i></button>
            </form>
        </section>
        <section class='recipe'>
            <section class="recipePt1">
                <img class="recipeImage" src="<%= recipe.image %>" alt="">
                <h2 class="recipeName"><%= recipe.name %></h2>
                <h3 class="recipeDescription"><%= recipe.description %></h3>
                <span class="recipePrep"><%= recipe.time.prep %></span>
                <span class="recipeCook"><%= recipe.time.cook %></span>
                <span class="recipeTotal"><%= recipe.time.total %></span>
                <span class="recipeServings"><%= recipe.servings %></span>
            </section>
            <section class="recipePt2">
                <span>Ingredients</span>
                <ul class='recipeIngredients'>
                <% for(let i=0; i < recipe.ingredients.length; i++) {%>
                    <li class='recipeIngredients'><%=recipe.ingredients[i] %></li>
                <% } %>
                </ul>
                <span>Directions</span>
                <ol class='recipeInstructions'>
                <% for(let i=0; i < recipe.instructions.length; i++) {%>
                    <li class='recipeInstructions'>
                        <%=recipe.instructions[i] %>
                    </li>
                <% } %>
                </ol>
            </section>
        </section>
    </section>
    



    <script src="js/main.js"></script>
</body>

</html>